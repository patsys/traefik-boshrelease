[entryPoints]
  [entryPoints.web]
    address = ":80"

  [entryPoints.web-secure]
    address = ":<%= p("traefik.tls.port") %>"

[providers]
  [providers.file]
    directory = "/var/vcap/jobs/traefik/conf/dist"
    watch = true

<% if p("traefik.api.enabled") == "true" -%>
[api]
<% if p("traefik.api.tls.port") != p("traefik.tls.port") -%>
  [entryPoints.api]
    address = ":<%= p("traefik.api.tls.port") %>"
<% end -%> 
<% end -%>
