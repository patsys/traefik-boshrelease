#!/usr/bin/env bash

set -e

mkdir -p "$BOSH_INSTALL_TARGET/bin"

tar -zxvf  traefik/traefik_v*_linux_amd64.tar.gz -C  "$BOSH_INSTALL_TARGET/bin" traefik 

chmod +x "$BOSH_INSTALL_TARGET/bin/traefik"
